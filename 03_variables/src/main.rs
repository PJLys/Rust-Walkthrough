fn main() {
    let mut x = 5;
    println!("The value of x is: {x}");
    x = 6;
    println!("The value of x is: {x}");
}

/*
    Shadowing
    = Reuse the same variable name, while changing the value and/or type of the data.
    Most likely use case: Reading from IOStream and converting the data into usable format.
*/
